{
  "name": "g4-skitrip",
  "version": "1.0.0",
  "description": "G4 Investments Ski Trip Landing Page",
  "scripts": {
    "build": "node build.js",
    "minify": "npm run build",
    "package": "npm run build && zip -r g4-skitrip-deployment.zip dist/",
    "deploy": "npm run package"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.24.0",
    "clean-css": "^5.3.2"
  }
}