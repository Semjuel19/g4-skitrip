{
  "name": "g4-skitrip",
  "version": "1.0.0",
  "description": "G4 Investments Ski Trip Landing Page",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist/ g4-skitrip-deployment.zip",
    "build:clean": "npm run clean && npm run build",
    "minify": "npm run build",
    "package": "npm run build:clean && zip -r g4-skitrip-deployment.zip dist/",
    "deploy": "npm run package"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.24.0",
    "clean-css": "^5.3.2"
  }
}